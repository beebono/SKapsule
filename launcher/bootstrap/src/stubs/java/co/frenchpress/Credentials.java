package co.frenchpress;

/**
 * Compile-only API stub of frenchpress's Credentials (a record there). Only the
 * (username, password) constructor NativeBridgePrompt calls is needed. NOT
 * packaged into sk-bootstrap.jar; the real record is on SK's classloader.
 */
public final class Credentials {
  public Credentials (String username, String password) {}
}
